# lib

Pure, framework-free utilities.

No React, no ORT, no DOM side effects beyond what a function is given. Home for
the export layer (JSON/CSV/clipboard/zip), slugification, and other shared
helpers that should be trivially unit-testable.

_Populated in Phase 10 and as needed._
